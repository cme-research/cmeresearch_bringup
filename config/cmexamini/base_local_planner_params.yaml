#For full documentation of the parameters in this file, and a list of all the
#parameters available for TrajectoryPlannerROS, please see
#http://www.ros.org/wiki/base_local_planner
DWAPlannerROS:
  #Set the acceleration limits of the robot
  acc_lim_theta: 2 #1.5
  acc_lim_x: 1.0
  acc_lim_y: 1.0

  #Set the velocity limits of the robot
  max_vel_x: 0.5
  min_vel_x: -0.5
  max_vel_y: 0.5
  min_vel_y: -0.5
  max_vel_theta: 1.57
  min_vel_theta: -1.57
  max_rotational_vel: 1.0
  min_in_place_vel_theta: 0.314

  #The velocity the robot will command when trying to escape from a stuck situation
  escape_vel: -0.1

  #For this example, we'll use a holonomic robot
  holonomic_robot: true

  #Since we're using a holonomic robot, we'll set the set of y velocities it will sample
  #y_vels: [-0.3, -0.1, 0.1, -0.3]

  #Set the tolerance on achieving a goal
  xy_goal_tolerance: 0.15
  yaw_goal_tolerance: 0.157
  latch_xy_goal_tolerance: false
  #latch_xy_goal_tolerance: true

  #We'll configure how long and with what granularity we'll forward simulate trajectories
  sim_time: 4.0
  sim_granularity: 0.025
  angular_sim_granularity: 0.02
  vx_samples: 20
  vtheta_samples: 40
  controller_frequency: 20.0

  #Parameters for scoring trajectories
  #pdist_scale: 0.75
  #gdist_scale: 1.0
  goal_distance_bias: 20.0 # this is called pdist_scale in this planner!
  path_distance_bias: 32.0 # this is called gdist_scale in this planner!
  occdist_scale: 0.02
  heading_lookahead: 0.325

  meter_scoring: true

  #We'll use the Dynamic Window Approach to control instead of Trajectory Rollout for this example
  dwa: true

  heading_scoring: false
  headking_scoring_timestep: 0.8
  simple_attractor: false
  publish_cost_grid_pc: true

  escape_reset_dist: 0.1
  escape_reset_theta: 0.1

  #How far the robot must travel before oscillation flags are reset
  oscillation_reset_dist: 0.05

  #Eat up the plan as the robot moves along it
  prune_plan: true
